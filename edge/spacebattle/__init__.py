"""Space-battle POC (`edge-spacebattle`) — a positioning-driven, turn-based
fleet-combat mini-game, candidate replacement for the stat-roll ship combat.

Standalone like `edge.groundwar`: pure `config`/`model`/`rules` under a
throwaway Textual shell (`app`), balance in `config/spacebattle_default.yaml`,
decisions in `docs/SPACEBATTLE_POC.md`. Inspired by the space combat of David
Weber's *In Fury Born*: vector movement, facing-keyed arcs and damage,
traveling missile salvos, fighter screens, and minefields laid in peacetime.
"""
