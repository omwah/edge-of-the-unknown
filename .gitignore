# Checked out reference code
references/**

# generated PDFs are build artifacts — never commit them
*.pdf
# pixi environments
.pixi/*
!.pixi/config.toml
__pycache__/
*.pyc
# Coverage.py
.coverage

# runtime server state (lobby accounts + hosted game saves)
accounts.db
games/

# snapshots unit test report file
snapshot_report.html

# Local process settings and secrets (server/sysop, edge-serve)
.env
